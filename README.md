# Shortcode_ROC

El plugin muestra un formulario al usuario ,mediante el shortcode [ROC_afiliados], en el cual introduce una URL de un producto de Amazon, y este le devuelve la URL modificada con el tag del afiliado de Amazon.
